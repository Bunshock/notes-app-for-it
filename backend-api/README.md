### Backend API


##### Installation

- Requirements: Java 21, Maven

- Steps:
    - Clone this repository:
    - Move to `backend-api` folder, and build the project:
    `mvn clean install`
    - Run with:
    `mvn spring-boot:run`